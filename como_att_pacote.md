* devtools::document() <- documenta as novas funções;
* devtools::check() <- checka pra erro e warning;
* devtools::install() <- instala no seu pc o pacote atualizado;
* usethis::use_version() <- ajuda a fazer o versionamento do pacote (adicionar os arquivos no stage antes de chamar essa função)